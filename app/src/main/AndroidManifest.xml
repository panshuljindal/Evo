<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.vit.gravitas">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Evo"
        android:usesCleartextTraffic="true">
        <activity android:name="edu.vit.gravitas.Activity.Notifications" />
        <activity android:name="edu.vit.gravitas.Activity.SeeAllActivity" />
        <activity android:name="edu.vit.gravitas.Activity.ClubActivity" />
        <activity android:name="edu.vit.gravitas.Activity.LandingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="edu.vit.gravitas.Activity.NavigationActivity"
            android:windowSoftInputMode="adjustPan">

        </activity>
        <activity android:name="edu.vit.gravitas.Activity.EventActivity" />
    </application>

</manifest>